@model IEnumerable<NikiShop.ToDo.WebApi.Models.ToDoList>

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <h2>Bienvenidos al curso de .NET</h2>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ToDoListId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ToDoListId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* ToDoListId=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* ToDoListId=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* ToDoListId=item.PrimaryKey */ })
                </td>
            </tr>
        }
    </tbody>
</table>